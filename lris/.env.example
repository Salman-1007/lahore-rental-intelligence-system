# Copy this file to `.env` and adjust values for your machine.
# Never commit the real `.env` file.

ENVIRONMENT=development

# Leave unset in development to use the default local SQLite file.
# In production, set a real PostgreSQL URL, e.g.:
# DATABASE_URL=postgresql://user:password@host:5432/lris
# DATABASE_URL=

LOG_LEVEL=INFO
