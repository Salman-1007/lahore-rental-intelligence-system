# TASKS.md — Granular Task Tracking

Convention: `[ ]` pending, `[x]` done, `[~]` in progress.
Update this file as part of the commit that completes each task — never
batch updates after the fact.

---

## M0 — Foundation

- [x] Create repository directory structure (`backend/`, `frontend/`, `data/`)
- [x] Write `PLAN.md`
- [x] Write `RULES.md`
- [x] Write `ARCHITECTURE.md`
- [x] Write `TASKS.md` (this file)
- [x] `backend/requirements.txt` with pinned core dependencies
- [x] `backend/app/core/config.py` — `Settings` (Pydantic BaseSettings)
- [x] `backend/app/core/logging_config.py` — structured logging setup
- [x] `backend/app/db/session.py` — engine/session factory (SQLite dev, Postgres prod)
- [x] `backend/app/main.py` — minimal FastAPI app factory (health check route only)
- [x] `pyproject.toml` — pytest / black / ruff / mypy configuration
- [x] `.gitignore` (data/, .env, __pycache__, model artifacts, venv)
- [x] `README.md` — project overview + local setup instructions
- [x] Verify: dependency install succeeds in a clean venv
- [x] Verify: FastAPI app boots and `/health` returns 200 (`test_health.py`, passing)
- [x] Verify: `pytest` runs and collects/passes the smoke test
- [ ] `backend/config/dev.yaml` / `prod.yaml` — deferred to M1, when there's
      actual per-environment config beyond what env vars already cover
- [ ] Git: initial commit (do this locally once you've pulled the scaffold in)

## M1 — Database Layer (complete)

- [x] `app/models/base.py` — declarative base + mixins (timestamps, id)
- [x] `app/models/enums.py` — PropertyType, PortionType, SizeUnit, Currency, SourceName, ScrapeStatus
- [x] `app/models/source.py` — `Source` model
- [x] `app/models/location.py` — `Location` + `LocationAlias` models (hierarchy + aliases)
- [x] `app/models/listing.py` — `Listing` model (aggregate root)
- [x] `app/models/property_details.py` — `PropertyDetails` model
- [x] `app/models/dimension.py` — `Dimension` model
- [x] `app/models/price_history.py` — `PriceHistory` model
- [x] `app/models/duplicate.py` — `Duplicate` model
- [x] `app/models/scrape_log.py` — `ScrapeLog` model
- [x] `app/models/__init__.py` — model registry for `create_all`/Alembic autogenerate
- [x] Alembic init, wired to app `Settings` + model metadata (`alembic/env.py`)
- [x] Initial migration autogenerated (`b50f0629db44_initial_schema.py`) — verified upgrade
      and downgrade both run cleanly against a real file-backed SQLite DB
- [x] `app/repositories/base_repository.py` — generic get/add/delete
- [x] `app/repositories/listing_repository.py` — dedup-aware lookup, price history
      append, filtered search (location/price/size/bedrooms) + tests
- [x] `app/repositories/location_repository.py` — hierarchy traversal, alias
      registration, RapidFuzz-based fuzzy search + tests
- [x] Integration tests against real in-memory SQLite (9/9 passing total,
      including the M0 smoke test)

**Verified for real:** `alembic upgrade head` / `alembic downgrade base`
both ran against `data/dev.db`; full pytest suite passes (9 passed).

## M2 — Scraper Framework + OLX (next milestone, not started)

---

Milestones M2–M12 will each get their own detailed task section here,
added at the start of that milestone (not drafted in advance, so tasks
reflect what M1's actual output makes necessary).

---

## M2–M11 — Full Backend + Frontend (complete)

Built in one pass; see `PLAN.md` for per-milestone status detail. Summary
of what shipped and what was verified:

- [x] `scrapers/base/` — `BaseScraper` template (pagination, retry+backoff
      via tenacity, rate limiting, resume via `ScrapeLog`), `RawListing` DTO
- [x] `scrapers/olx/scraper.py`, `scrapers/zameen/scraper.py` — real
      scraper implementations (selectors are best-effort; this sandbox
      cannot reach olx.com.pk/zameen.com to verify live, see RUNBOOK.md)
- [x] `scrapers/graana/scraper.py` — placeholder, not yet implemented
- [x] `pipeline/parser`, `pipeline/cleaner`, `pipeline/normalizer`,
      `pipeline/dedup` — full stage implementations + unit/integration tests
- [x] `app/services/ingestion_service.py` — orchestrates all 4 stages +
      repository upsert per scraped listing
- [x] `ml/features/feature_engineering.py` — leakage-safe K-fold location
      target encoding (documented design decision: `price_per_marla` is
      EDA-only, never a model input, since it's derived from the target)
- [x] `ml/datasets/dataset_builder.py` — immutable, timestamped
      raw/clean/processed/training/validation versioning
- [x] `ml/eda/eda_report.py` — automated markdown EDA report
- [x] `ml/training/train.py` — CatBoost, K-fold CV hyperparameter search,
      early stopping, main + two quantile models, dataset-version linkage
- [x] `ml/evaluation/evaluate.py` — MAE/RMSE/R²/MAPE + persisted report
- [x] `app/services/prediction_service.py` — loads latest model, builds
      leakage-consistent feature vectors, returns estimate/confidence/range
- [x] All 6 required endpoints: `/search`, `/predict`, `/autocomplete`,
      `/listings/{id}`, `/stats`, `/trends`, plus auto `/docs`
- [x] React frontend (Vite): Search, Estimate, Insights, Listing-detail
      pages; "Land Registry" design direction

**Verified for real:**
- Full backend test suite: 35/35 passing (unit, integration, API, and an
  end-to-end ML smoke test that runs dataset-build → CatBoost train →
  predict against synthetic fixture data)
- `alembic upgrade head` / `downgrade base` still clean after schema
  additions
- Frontend: `npm install` + `npm run build` succeed; `vite preview` serves
  the built app
- **Not verified:** live scraping against real OLX/Zameen pages, and
  training against real scraped data — both require running outside this
  sandbox (see `RUNBOOK.md`)
